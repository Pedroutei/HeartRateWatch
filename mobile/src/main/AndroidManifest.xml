<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:roundIcon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Material.Light.NoActionBar">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".RunHistoryActivity"
            android:exported="false" />

        <activity
            android:name=".LeaderboardActivity"
            android:exported="false" />

        <!-- Receives the "/alert/..." messages the watch sends the instant a threshold is
             crossed (plays the alert sound here on the phone, see AlertPlayer), the
             "/calibration/..." message sent when a guided max-HR test is saved, and the
             "/run/..." message sent when a run stops. -->
        <service
            android:name=".WearMessageListenerService"
            android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/alert" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/calibration" />
                <data
                    android:scheme="wear"
                    android:host="*"
                    android:pathPrefix="/run" />
            </intent-filter>
        </service>

    </application>

</manifest>
